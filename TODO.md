# TODO

- [x] Update `src/pages/DashboardPage.jsx` to fetch logged-in user profile via `profileApi.getMy()` and render real profile data instead of `profilePreview` mock data.
- [x] Add loading/error handling and remove mock-based “Live card preview” fields.
- [ ] Keep analytics/metrics mock UI unchanged unless backend integration is already available.
- [x] Run frontend build/dev check (lint + compile) to ensure no runtime errors.



